[project]
name = "llm-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "orjson>=3.11.5",
    "mistralai>=1.10.0",
]

[dependency-groups]
dev = [
    "uvicorn>=0.40.0",
]
